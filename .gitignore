# 依赖
node_modules/

# pnpm 的本地内容寻址存储（install 时落在仓库根，勿入库）
.pnpm-store/

# 日志与临时文件
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# 环境变量
.env
.env.*

# 本地 TLS/SSH 工具文件(沙箱环境使用)
ca-bundle.pem
keys/
.ssh/

# 本地开发协作规范(不入库)
DEVELOPMENT.md

.obsidian/

.mnemon/

doc/

# 智能体协作面（不入库；2026-09-24 由人类所有者裁定：「docs 目录下的都不能提交」）
docs/agents/*
# 例外：集成契约是 CI 门的输入，必须留在版本库 —— 否则干净检出里锚门直接失败。
#   证据：.github/workflows/npm-publish.yml:91（§0 内容锚门：重算本文件 §0 的 12 条锚）
#         scripts/anchor.contract.selftest.mjs:47（ANCHOR_DEFAULT_REL 默认指向本文件）
!docs/agents/integration-contracts.md